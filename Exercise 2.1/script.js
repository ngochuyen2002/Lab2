// script.js
function submitForm() {
  // Get the value from the input box
  var inputValue = document.getElementById("infoInput").value;

  // Display a pop-up with the filled-in information
  alert("Successful! Information: " + inputValue);
}
